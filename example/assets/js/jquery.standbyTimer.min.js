/*
 * jQuery standbyTimer
 * Version 0.1 (@edouardkombo / breezeframework.com)
 * https://github.com/edouardkombo/jquery-standbyTimer
 *
 * Initialize a timer that starts by clicking on an element of the page.
 * If a click has been detected before the end of the timer, timer is increased.
 * Timer is resetted if no click has been detected before its end.
 *
 * Copyright (c) 2014 Edouard Kombo (@edouardkombo / breezeframework.com)
 * Dual licensed under the MIT and GPL licenses.
*/
(function(e){e.standbyTimer=function(t,n){var r={timer:120,interval:1e3,onComplete:null,stop:false,debug:false};if(n){e.extend(r,n)}ghostTimer=0;clickOnSelector=0;var i=setInterval(function(){if(ghostTimer>0){++ghostTimer}if(ghostTimer>=r.timer){ghostTimer=1;if(r.stop===true){clearInterval(i)}if(clickOnSelector===1){clickOnSelector=0;if(e.isFunction(r.onComplete)){r.onComplete.call(this)}}}e(t).click(function(){if(clickOnSelector===0){clickOnSelector=1;ghostTimer=1}});if(r.debug===true){console.log("Timer:"+ghostTimer+" // ClickOnPage:"+clickOnSelector)}},r.interval);return this}})(jQuery)